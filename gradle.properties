org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Real HTTPS/HTTP guide when GUIDE_API_BASE is set (or entered in-app).
# Empty GUIDE_API_BASE → mock fallback. USE_MOCK_GUIDE=true forces mock.
USE_MOCK_GUIDE=false
GUIDE_API_BASE=
